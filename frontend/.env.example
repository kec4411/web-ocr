# Base URL of the OCR backend.
# Vite inlines VITE_* at BUILD time, so the production image needs this as a
# build arg (see frontend/Dockerfile), not just a runtime env var.
VITE_API_BASE_URL=http://localhost:9004
